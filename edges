X	files don't exist
	empty crap

X	> If the program is given a -i flag loaded from a table that doesn't fitn
    > it (say if that table was created with -m 20), should it reject it as
    > invalid?  (Your program gives an "invalid entry" error).

X   decode -o -> to encode -i 

    fread and fwrite it yo!

    argument checks!